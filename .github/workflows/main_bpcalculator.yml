name: Deploy BPCalculator to Azure

on:
  # 🔁 Trigger automatically AFTER BPCalculator CI finishes
  workflow_run:
    workflows: ["BPCalculator CI"] # 👈 must match your CI workflow name
    types:
      - completed

  # (Optional) still allow manual deploy from Actions tab
  workflow_dispatch:

jobs:
  deploy:
    # ✅ Only run if CI succeeded AND branch is main
    if: >
      ${{ github.event.workflow_run.conclusion == 'success' &&
          github.event.workflow_run.head_branch == 'main' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code for the tested commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Install prod dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction --no-progress

      - name: Deploy to Azure Web App (student subscription)
        uses: azure/webapps-deploy@v3
        with:
          app-name: "bpcalculator" # 👈 your app name
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .
